import React from 'react';
import useTitle from '../../hook/useTitle';

const Blog = () => {
    useTitle("Blog")


    return (
        <div>
            <h2>this is blog page</h2>
        </div>
    );
};

export default Blog;